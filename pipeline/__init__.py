"""Decoupled multi-view pipeline modules.

Each step is a thin wrapper around an existing function in the parent
``golf-hand-object`` codebase (now copied here). The Gradio app in ``app.py``
drives them step by step.
"""
